# minichess-AI-1

## main build

### make 'build' directory

At the first time build, you should run

```
mkdir build
```

### build

First, move corrent directly to 'build'

```
cd build
```

update Makefile

```
cmake ..
```

build

```
cmake --build .
```
